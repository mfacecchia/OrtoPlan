<nav>
    <a href = "/"><img src = "/assets/icons/favicon_wide.svg"></a>

    <div>
        <% if(typeof showDropdownMenus !== 'undefined'){ %>
            <!-- TODO: Add user & notifications dropdown -->
        <% } else{ %>
            <a <% if(typeof isSignupPage !== 'undefined'){ %> class = "text-primary" <% } %> href = "/signup" title = "Create an account">Sign-up</a>
            <a <% if(typeof isLoginPage !== 'undefined'){ %> class = "text-primary" <% } %> href = "/login" title = "Log-in into your account">Log-in</a>
        <% } %>
    </div>
</nav>